import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.nicholasgriffin.personalassistant",
	appName: "ng-personal-assistant",
	webDir: "dist",
};

export default config;
